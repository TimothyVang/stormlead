[project]
name = "stormlead-landing"
version = "0.1.0"
description = "StormLead local landing page"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "jinja2>=3.1",
    "uvicorn[standard]>=0.32",
]

[tool.uv]
package = false
